@model IEnumerable<Pr3Obligatorio_AAN2023.Models.Reserva>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Asiento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
            <th>
                    @Html.DisplayNameFor(model => model.Usuario.Nombre) <!-- Agregar el nombre de la propiedad del usuario -->
            </th>
            <th>
                    @Html.DisplayNameFor(model => model.Funcion.Pelicula) <!-- Agregar el nombre de la propiedad de la función -->
            </th>
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Asiento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Usuario.Nombre) <!-- Mostrar el nombre del usuario -->
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Funcion.Pelicula) <!-- Mostrar el nombre de la función -->
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
